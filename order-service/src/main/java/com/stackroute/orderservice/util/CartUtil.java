/*
package com.stackroute.orderservice.util;

import com.stackroute.orderservice.dto.CartDetails;
import com.stackroute.orderservice.model.Cart;
import org.springframework.stereotype.Component;


@Component
public class CartUtil {

   public CartDetails toCartDetails(Cart cart){
        CartDetails cartDetails=new CartDetails();
       cartDetails.setCustomerEmailId(cart.getCustomerEmailId());
       cartDetails.setCartAddedDated(cart.getCartAddedDated());
       cartDetails.setDesignList(cart.getDesignList());
       cartDetails.setDiscount(cart.getDiscount());
       cartDetails.setTotalAmount(cart.getTotalAmount());

       return cartDetails;
    }
}
*/
